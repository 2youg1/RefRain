# 路线图

*[English](ROADMAP.md)*

---

## 已发布 — v0.1.2

Windows x64。一位作者、多个本地文件夹、任意 Harness、章节化 Markdown。

一个版本必须走通、且已经走通的路径：

```
打开文件夹 → 写作 → 选一段、写要求、挑 Agent
→ 加入待发队列 → 一次点击送出
→ Harness 执行并写出 Result Artifact
→ 程序冻结成 Proposal 与 Review Slice → 人逐句裁决
→ Decision Batch 产生新的 Text Head → 裁决进入账本
```

程序里现在还有：多根工作区与单文件打开；每一处改动都可单独撤回的修改记录；
作为唯一常驻控件的命令面板；十八项排版控制，中西文字体分开设置；五款内嵌
OFL 字体；三套主题；可重绑的快捷键；精确落在字下的基线网格；行号、小地图与
渐变进度条；会告诉你 Harness 到底连没连上的 Agent 面板。

## 下一步 — v0.2

**画布版式。** 每篇一个窗口、自由排布、可放大成完整编辑面。已设计未实现；
设置里那个开关是禁用并标注过的，不会让人误以为它在工作。

**真正的编辑器内核。** 正文目前是带段落元素的 `contenteditable`。写散文够用，
但撑不住一万次操作之后的选择性撤销，也撑不住锚定在区间上、且能在编辑后存活的
装饰。ProseMirror 会垫到下面，验收标准是 IME 门禁。

**搜索。** 跨整个工作区，而不只是当前打开的这一章。

**页眉对齐缺陷。** 章节页眉无法与正文共用左边缘，原因未明。记在 SPEC §12 Q5，
每次截图运行都会测量并报告。

## 更远

**账本上的检索。** 裁决无论有没有人读都在累积；让它们可检索，才是把记录变成
品味的那一步。

**更多 L2 适配器。** 每一个都是对真实会话跑一遍契约测试，而不是表格里的一句
声明。

**大规模选择性撤销。** 在一万次互不相交的改动之后，撤销其中的第一次，而不必
重放整段历史。

## 不做

多人协作。云同步。插件加载器。这里任何一项都会要求程序发起网络请求、保存账号、
或运行并非自己发布的代码——而 README 里那些拒绝之所以有价值，正在于它们是无条件的。

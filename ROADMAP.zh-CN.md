# 路线图

*[English](ROADMAP.md)*

---

## 已发布 — v0.1.5

Windows x64。一位作者，若干本地文件夹，任意 harness，分章 Markdown。

这一版处理的是稿子经过这个应用之后还剩下什么。

**你没动过的字节，原样留在原处。** 从前打开一份稿子、一个字不改直接保存，字节就会少：中文段落开头的全角缩进在载入时被剥掉，围栏代码块被它内部的空行切成两段，连续空行被压成一个。现在文件解析成的每个块都记得自己在原文里的位置，保存只替换真正改过的区间。这是下界而非无损 Markdown——你重写过的段落仍然按你重写的样子写回——由 `verify:roundtrip` 用二十份语料把守。

**打中文时会等输入法把词打完。** 渲染层此前对组合期一无所知：拼到一半按 Ctrl+S，未上屏的拼音就被当作正文落盘；组合期间的任何重绘，会把输入法正在写入的节点整个换掉。现在组合期的保存被推迟而不是被拒绝。正文表面也不再折叠空白，缩进因此看得见。

**保存长篇的代价，取决于改动多大，而不是书有多长。** 四万块的稿子按一次保存就抛 `RangeError: Out of memory`——因为对齐表在比较任何内容之前就已分配。两万块的稿子改一个字要 5.2 秒，现在是 5.5 毫秒；十万块在从前全部崩溃的六种改动形态下都能跑完。

## 更早发布 — v0.1.3

Windows x64、Linux x64、macOS arm64 与 x64。一位作者，若干本地文件夹，任意 harness，分章 Markdown。

一次发布必须走通、且确实走通的路径：

```
打开文件夹 -> 写作 -> 选一段、写指令、挑一个 agent
-> 排队 -> 一次点击派发整批
-> harness 运行并写出结果工件
-> 应用冻结提案与检阅切片 -> 人来裁决
-> 裁决批产生新的正文头 -> 裁决进入账本
```

应用现在还有：多个工作区根目录与单文件打开；逐次改动的记录与按条撤销；作为唯一常驻控件的命令面板；十八项排版控制，中西文字体分别设置；五款随包的 OFL 字体；可重绑的快捷键；落在字下而非穿过字的行线；行号、缩略图与进度渐变；能报告 harness 是否真的连得上的 agent 面板。

**原生文件层**（`packages/fs`）：遍历、搜索、排序，以及只进系统回收站、任何一层都没有永久删除变体的删除。Rust 经 N-API 暴露，在哪个平台运行就在哪个平台构建。

## 已知边界

写在这里而不是塞进 issue，因为用户第一天就会遇到。

**尚未在真机验证。** CI 在四个目标上都能构建并启动，但 Windows 输入法门禁（`e2e/ime`）需要真实显示器与真实输入法，macOS 回收站绑定需要真实的 Mac。这两项结果都不得从 Linux runner 推断。

**Claude Code 目前是 L1，不是 L2。** 它的协议与生命周期有进程测试覆盖。SPEC §6.5 要求真实会话与 compaction 信号才能称 L2，这份证据还不存在。

**卷上没有可写回收站时删除会失败。** 若卷根目录只读，freedesktop 规范无法创建 `.Trash-<uid>`。此时操作失败、文件留在原处，界面会指名说明。退回永久删除会毁掉这一层存在的理由。

**发布产物未签名。** Windows SmartScreen 与 macOS Gatekeeper 会提示。

**整份接受一个提案而不裁决其切片，不会产生有用结果。** 记为 SPEC §12 Q6，产品决定尚未作出。

## 下一步 — v0.2

**真正的编辑器内核。** 正文目前是一个装着段落元素的 `contenteditable`。这对散文够用，但不足以支撑一万次操作范围内的选择性撤销，也不足以支撑能在编辑后存活的区间装饰。ProseMirror 将垫到下面，以输入法门禁作为验收测试。

**引导式 harness 接入。** agent 面板能说出某个命令连不上，但还不能发现机器上已经装了哪些 harness。

**重启后的持久化。** agent 名册已落到 `.refrain/agents.json`，含命令模板——那是这里唯一无法由磁盘重建的东西。队列与未合并提案仍在内存，应当从已经在磁盘上的 `.refrain/runs/` 重建：结果文件都在，重开项目本该自己找回未做完的活，而不是被告知它丢了。

**画布布局。** 每篇一个窗口，自由排布，可缩放至完整编辑面。已设计未构建；设置里的开关处于禁用状态并已标注，以免被误认为可用。

**跨工作区搜索，** 而不只是当前打开的章节。

## 更远

**账本的检索。** 裁决无论是否被读取都在累积；让它可检索，才把记录变成品味。

**更多 L2 harness 适配器。** 每一个都是针对真实会话的契约测试，不是表格里的一句声明。

**规模化的选择性撤销。** 对一万次互不相交的编辑中的第一次施加补偿行动，且不回放历史。

**无损 Markdown，而不只是下界。** v0.1.5 保证你没有编辑过的字节原样回来。更强的那个承诺——你**改写过**的段落也能在每一种 Markdown 构造上往返——需要一个真正的文档模型，因此归在编辑器内核之后，而不是之前。

## 不打算做

多人协作。云同步。插件加载器。这三者中的任何一个都会要求应用发出网络请求、保存账户，或运行并非随包出厂的代码——而 README 里那些拒绝之所以有价值，正因为它们是无条件的。

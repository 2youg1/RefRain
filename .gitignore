node_modules/
dist/
.DS_Store
*.log

# Local design authority. `verify:no-spec-upload` enforces this boundary.
/SPEC.md

# 本地评审工件：版面对比页由 scripts/generate-layout-preview.ts 重建。
# 脚本进仓库，产物不进——它是给人看一眼做裁决用的，不是发布内容。
/docs/layout-preview.html

# 同上：面板栈的渲染截图由 scripts/render-panel-preview.ts 重建。
# 截图记录外观，不记录产生外观的值——所以脚本进仓库，图不进。
/docs/panel-preview/

# 同上：语义断行开关对比页由 scripts/build-semantic-break-compare.ts 重建。
/docs/semantic-break-compare.html

# Rust and Tauri generated output.
target/
apps/desktop/src-tauri/gen/

# Legacy Electron/Svelte output. Remove each rule with its legacy module.
legacy/packages/fs/*.node
legacy/apps/desktop/release/
legacy/apps/desktop/build/checks/
legacy/e2e/ime/results/
legacy/e2e/ime/page/editor.html
legacy/e2e/ime/page/editor.bundle.js
legacy/e2e/ime/shells/wv2/wv2pkg/

# TypeScript incremental build state.
*.tsbuildinfo

# Probe evidence: regenerated by scripts/probe-*.ts, not reviewed as source.
probe-results/
packages/editor/.probe-entry.ts
e2e/ime/results/
e2e/ime/page/editor.html
e2e/ime/page/dist/
e2e/ime/shells/wv2/wv2pkg/

# 测试语料是 scripts/freeze-corpora.ts 的产物，构建与门禁前自动生成。
# 权威是那个脚本，不是磁盘上的字节（KL9 2026-07-30：上传不该包含这些）。
/tests/corpora/*.md
